interface:
  display_name: "Mi Notes"
  short_description: "Repo-specific patterns and workflows for mi-notes"
  default_prompt: "Use the mi-notes repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true